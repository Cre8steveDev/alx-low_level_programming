/**
 * main - Entry point
 * Description: A function to print string with printf
 * Return: Always 0 (Success)
 */
#include <stdio.h>
int main(void)
{
printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
return (0);
}
