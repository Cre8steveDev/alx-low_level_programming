#include "main.h"

/**
 * _isupper - Function checks if char is upper or not
 * @c: Only parameter in function
 * Return: Returns 1 if true 0 if false
*/

int _isupper(int c)
{
	if (c >= 65 && <= 90)
		return (1);
	return (0);
}
