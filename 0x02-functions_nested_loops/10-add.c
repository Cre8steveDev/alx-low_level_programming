#include "main.h"
/**
 * add - Adds two integers todether and returns the result
 * @n: first operand
 * @o: second operand
 *
 * Return: Always int (Success)
 */

int add(int n, int o)
{
		return (n + o);
}
